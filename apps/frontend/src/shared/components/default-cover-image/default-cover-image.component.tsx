'use client';

import Box from '@mui/material/Box';
import { styled } from '@mui/material/styles';

export const DefaultCoverImage = styled(Box)({
  background: 'radial-gradient(circle at 50% 0%, #02F079, #202124)',
  height: 140,
  borderRadius: `8px 8px 0 0`,
});
