# Create/Add new strategies

Open the integrated VSCode terminal and issue this commands, For sake of simplicity I used to configure the `local-strategy`:

-   Create the configuration file: `nest g service packages/auth/local-auth --no-spec`
-   Create the Guard: `nest g guard packages/auth/local-auth/local-auth`
